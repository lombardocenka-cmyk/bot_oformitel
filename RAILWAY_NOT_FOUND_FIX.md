# 🔧 Исправление ошибки "Not Found" в Railway

## Проблема
При переходе по домену показывается "Not Found" - это означает, что приложение не запустилось.

## Решение

### 1. Проверьте статус деплоя

1. В Railway нажмите на ваш сервис "web"
2. Перейдите на вкладку **"Deployments"**
3. Проверьте последний деплой:
   - ✅ **"Active"** (зеленый) - деплой успешен
   - ❌ **"Failed"** (красный) - есть ошибки
   - ⏳ **"Building"** или **"Deploying"** - еще идет

### 2. Если деплой Failed - проверьте логи

1. Нажмите на деплой с ошибкой
2. Нажмите **"View Logs"** или **"Logs"**
3. Посмотрите, какая ошибка

**Частые ошибки:**
- `ModuleNotFoundError` - не все зависимости установлены
- `Port already in use` - неправильный порт
- `Command not found` - неправильная команда запуска

### 3. Проверьте команду запуска

В Railway → Settings → Start Command должно быть:

```
cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT
```

**ИЛИ** если у вас есть `Procfile`, Railway должен использовать его автоматически.

### 4. Проверьте переменные окружения

В Railway → Variables должны быть:
- `BOT_TOKEN`
- `ADMIN_ID`
- `CHANNEL_ID`
- `WEBAPP_URL` (опционально, но лучше добавить)

### 5. Проверьте, что Procfile существует

Убедитесь, что в корне проекта есть файл `Procfile` с содержимым:
```
web: cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT
```

### 6. Перезапустите деплой

1. В Railway → Deployments
2. Нажмите на три точки (⋮) рядом с последним деплоем
3. Выберите **"Redeploy"**

## 🔍 Диагностика

**Если деплой Active, но все равно Not Found:**

1. Проверьте логи в реальном времени:
   - Railway → ваш сервис → вкладка "Logs"
   - Должны быть сообщения от uvicorn

2. Проверьте, что приложение слушает правильный порт:
   - Railway использует переменную `$PORT`
   - Убедитесь, что в команде используется `--port $PORT`

3. Проверьте структуру файлов:
   - Должна быть папка `webapp/`
   - В ней должны быть `server.py` и `static/`

## 📝 Быстрое решение

Если ничего не помогает, попробуйте:

1. **Удалите и создайте сервис заново:**
   - Railway → Settings → Delete Service
   - Создайте новый сервис из GitHub репозитория

2. **Или обновите код:**
   ```powershell
   git add .
   git commit -m "Fix deployment"
   git push
   ```
   Railway автоматически задеплоит заново

## ❓ Что проверить в первую очередь:

1. ✅ Статус деплоя (Active/Failed)
2. ✅ Логи деплоя (если Failed)
3. ✅ Команда запуска в Settings
4. ✅ Наличие Procfile
5. ✅ Переменные окружения

Пришлите скриншот логов деплоя, если не получается исправить!

