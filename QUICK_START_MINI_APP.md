# Быстрый старт Mini App

## 🚀 Быстрая установка

```powershell
# 1. Установите зависимости
py -3.11 -m pip install fastapi uvicorn python-multipart

# 2. Настройте .env
# Добавьте в .env:
# WEBAPP_URL=https://ваш-домен.com
# (Для разработки используйте ngrok)
```

## 📱 Запуск

### Вариант 1: С ngrok (для разработки)

**Терминал 1: Бот**
```powershell
py -3.11 main.py
```

**Терминал 2: Веб-сервер**
```powershell
cd webapp
py -3.11 start_server.py
```

**Терминал 3: ngrok**
```powershell
ngrok http 8000
# Скопируйте HTTPS URL и добавьте в .env как WEBAPP_URL
```

### Вариант 2: Без ngrok (только бот)

Если не хотите использовать Mini App, просто запустите бота:
```powershell
py -3.11 main.py
```

Бот будет работать в обычном режиме через команды.

## ✅ Проверка

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Должна появиться кнопка "📱 Открыть приложение"
4. Нажмите на неё - откроется Mini App

## 🔧 Решение проблем

**Mini App не открывается?**
- Проверьте, что веб-сервер запущен
- Убедитесь, что URL в `.env` правильный и начинается с `https://`
- Проверьте, что ngrok работает (если используете)

**Ошибка "Invalid init data"?**
- Проверьте `BOT_TOKEN` в `.env`

Подробная инструкция: см. `MINI_APP_SETUP.md`

