# ⚠️ Проблема: Limited Access в Railway

## Проблема

Railway показывает "Limited Access" - ваш аккаунт на ограниченном плане и может деплоить только базы данных, не приложения.

## Решения

### Вариант 1: Использовать другой бесплатный сервис (рекомендуется)

#### 🚀 Render.com (бесплатно)

1. **Зарегистрируйтесь:** https://render.com
2. **Создайте новый Web Service:**
   - New → Web Service
   - Connect GitHub → выберите `bot_oformitel`
3. **Настройки:**
   - **Name:** `telegram-miniapp`
   - **Environment:** `Python 3`
   - **Build Command:** (оставьте пустым)
   - **Start Command:** `cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT`
4. **Переменные окружения:**
   - `BOT_TOKEN`
   - `ADMIN_ID`
   - `CHANNEL_ID`
5. **Домен:** Render автоматически даст домен типа `telegram-miniapp.onrender.com`

#### 🌐 Fly.io (бесплатно)

1. **Установите Fly CLI:**
   ```powershell
   # Windows
   iwr https://fly.io/install.ps1 -useb | iex
   ```

2. **Войдите:**
   ```powershell
   fly auth login
   ```

3. **Создайте приложение:**
   ```powershell
   cd "V:\Vladik Botik"
   fly launch
   ```

4. **Настройте переменные:**
   ```powershell
   fly secrets set BOT_TOKEN=ваш_токен
   fly secrets set ADMIN_ID=ваш_id
   fly secrets set CHANNEL_ID=@ваш_канал
   ```

#### ☁️ PythonAnywhere (бесплатно)

1. **Зарегистрируйтесь:** https://www.pythonanywhere.com
2. **Загрузите файлы** через веб-интерфейс
3. **Настройте веб-приложение** в панели управления
4. **Добавьте переменные окружения**

### Вариант 2: Обновить план Railway (платно)

1. В Railway нажмите на желтый баннер "Upgrade your plan"
2. Выберите план (от $5/месяц)
3. После обновления сможете деплоить приложения

### Вариант 3: Использовать только бота (без Mini App)

Если не хотите настраивать веб-сервер:
- Просто запустите бота локально: `py -3.11 main.py`
- Все функции работают через команды бота
- Mini App не нужна

## 🎯 Рекомендация

**Используйте Render.com** - это самый простой и бесплатный вариант:
- ✅ Бесплатно
- ✅ Автоматический HTTPS
- ✅ Простой деплой из GitHub
- ✅ Автоматические обновления

## 📝 Инструкция для Render

1. Зарегистрируйтесь на https://render.com
2. New → Web Service
3. Connect GitHub → `bot_oformitel`
4. Настройки:
   - Start Command: `cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT`
5. Добавьте переменные окружения
6. Deploy!

Подробная инструкция: см. `RENDER_DEPLOY.md` (создам по запросу)

