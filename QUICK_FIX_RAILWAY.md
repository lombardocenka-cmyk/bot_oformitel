# ⚡ Быстрое решение проблемы Railway

## Проблема
Railway показывает "Limited Access" - бесплатный план не позволяет деплоить приложения.

## ✅ Самое быстрое решение: Render.com

### За 5 минут:

1. **Зарегистрируйтесь:** https://render.com (через GitHub)

2. **Создайте Web Service:**
   - New → Web Service
   - Connect GitHub → `bot_oformitel`
   - Start Command: `cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT`

3. **Добавьте переменные:**
   - `BOT_TOKEN`
   - `ADMIN_ID`
   - `CHANNEL_ID`

4. **Deploy!** - Render автоматически даст домен

5. **Добавьте `WEBAPP_URL`** с полученным доменом

6. **Готово!** Mini App работает

## 📋 Или используйте только бота

Если не хотите настраивать веб-сервер:
```powershell
py -3.11 main.py
```
Все функции работают через команды бота, Mini App не обязательна.

## 🎯 Что выбрать?

- **Render.com** - если нужна Mini App (бесплатно, но может "засыпать")
- **Только бот** - если Mini App не критична (работает всегда)

Подробная инструкция для Render: см. `RENDER_DEPLOY.md`

