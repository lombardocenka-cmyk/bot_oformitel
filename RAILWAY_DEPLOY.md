# 🚂 Деплой Mini App на Railway

## Шаг 1: Подготовка репозитория

### Если у вас еще нет Git репозитория:

1. **Инициализируйте Git в проекте:**
```powershell
cd "V:\Vladik Botik"
git init
git add .
git commit -m "Initial commit"
```

2. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Создайте новый репозиторий (например: `telegram-miniapp-bot`)
   - **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть файлы)

3. **Подключите локальный репозиторий к GitHub:**
```powershell
git remote add origin https://github.com/ваш-username/telegram-miniapp-bot.git
git branch -M main
git push -u origin main
```

### Если репозиторий уже есть:
Просто убедитесь, что все файлы закоммичены и запушены:
```powershell
git add .
git commit -m "Add Mini App files"
git push
```

## Шаг 2: Подключение к Railway

1. **Войдите в Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите **"New Project"**
   - Выберите **"Deploy from GitHub repo"**
   - Выберите ваш репозиторий `telegram-miniapp-bot`
   - Railway автоматически определит проект

## Шаг 3: Настройка проекта

### 3.1. Настройте переменные окружения

1. В проекте Railway нажмите на ваш сервис
2. Перейдите на вкладку **"Variables"**
3. Добавьте следующие переменные:

```
BOT_TOKEN=ваш_токен_бота
ADMIN_ID=ваш_telegram_id
CHANNEL_ID=@ваш_канал_или_id
WEBAPP_URL=https://ваш-проект.up.railway.app
DATABASE_PATH=bot_database.db
```

**⚠️ ВАЖНО:** `WEBAPP_URL` сначала оставьте пустым, мы добавим его после получения домена.

### 3.2. Настройте команду запуска

1. Перейдите на вкладку **"Settings"**
2. Найдите раздел **"Build Command"** (может быть пустым)
3. Найдите раздел **"Start Command"** и установите:

```bash
cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT
```

Или создайте файл `Procfile` в корне проекта (см. ниже).

## Шаг 4: Создайте необходимые файлы

### Создайте `Procfile` в корне проекта:

```procfile
web: cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT
```

### Создайте `runtime.txt` (опционально, для указания версии Python):

```
python-3.11.1
```

### Обновите `requirements.txt` (если нужно):

Убедитесь, что все зависимости указаны:
```
aiogram==3.22.0
aiohttp>=3.9.0,<3.13
aiosqlite>=0.19.0
beautifulsoup4>=4.12.3
lxml>=5.1.0
python-dotenv>=1.0.1
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
```

## Шаг 5: Настройка для работы с базой данных

Railway предоставляет временное хранилище, но для продакшена лучше использовать PostgreSQL.

### Вариант A: Использовать встроенную базу данных (для начала)

1. В Railway добавьте сервис **PostgreSQL** (опционально)
2. Или используйте SQLite (файл будет сбрасываться при перезапуске)

### Вариант B: Использовать PostgreSQL (рекомендуется)

1. В Railway нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст переменные:
   - `DATABASE_URL`
   - `PGHOST`
   - `PGPORT`
   - и т.д.

3. Обновите `database.py` для поддержки PostgreSQL (см. ниже)

## Шаг 6: Получение домена

1. В Railway перейдите на вкладку **"Settings"**
2. Найдите раздел **"Domains"**
3. Нажмите **"Generate Domain"**
4. Railway создаст домен типа: `ваш-проект.up.railway.app`
5. Скопируйте этот URL

6. **Обновите переменную окружения:**
   - Вернитесь в **"Variables"**
   - Обновите `WEBAPP_URL` на полученный URL (с `https://`)

## Шаг 7: Деплой

1. **Закоммитьте и запушьте изменения:**
```powershell
git add .
git commit -m "Configure for Railway deployment"
git push
```

2. **Railway автоматически начнет деплой:**
   - Перейдите на вкладку **"Deployments"**
   - Следите за процессом деплоя
   - Дождитесь статуса **"Active"**

3. **Проверьте логи:**
   - Нажмите на деплой
   - Просмотрите логи на наличие ошибок

## Шаг 8: Проверка работы

1. **Откройте ваш домен в браузере:**
   ```
   https://ваш-проект.up.railway.app
   ```
   Должна открыться страница Mini App

2. **Проверьте статические файлы:**
   ```
   https://ваш-проект.up.railway.app/static/style.css
   ```

3. **Проверьте в Telegram:**
   - Откройте бота
   - Отправьте `/start`
   - Нажмите "📱 Открыть приложение"
   - Mini App должна открыться

## Шаг 9: Настройка бота (локально)

Обновите ваш локальный `.env` файл:

```env
BOT_TOKEN=ваш_токен
ADMIN_ID=ваш_id
CHANNEL_ID=@ваш_канал
WEBAPP_URL=https://ваш-проект.up.railway.app
```

Запустите бота локально:
```powershell
py -3.11 main.py
```

## 🔧 Решение проблем

### Ошибка "Module not found"
- Убедитесь, что все зависимости в `requirements.txt`
- Проверьте логи деплоя

### Ошибка "Port not found"
- Railway автоматически устанавливает переменную `$PORT`
- Убедитесь, что в команде запуска используется `$PORT`

### Mini App не открывается
- Проверьте, что `WEBAPP_URL` правильный и начинается с `https://`
- Убедитесь, что деплой успешен (статус "Active")
- Проверьте логи на наличие ошибок

### База данных не работает
- SQLite файлы сбрасываются при перезапуске на Railway
- Используйте PostgreSQL для постоянного хранения

## 📝 Дополнительные настройки

### Настройка кастомного домена (опционально)

1. В Railway → Settings → Domains
2. Нажмите "Custom Domain"
3. Добавьте ваш домен (например: `miniapp.ваш-домен.ru`)
4. Настройте DNS записи как указано в Railway
5. Обновите `WEBAPP_URL` в переменных окружения

### Автоматические обновления

Railway автоматически деплоит при каждом push в GitHub:
```powershell
git add .
git commit -m "Update"
git push
```

## ✅ Готово!

Теперь ваш Mini App доступен по адресу:
`https://ваш-проект.up.railway.app`

И автоматически обновляется при каждом push в GitHub!

