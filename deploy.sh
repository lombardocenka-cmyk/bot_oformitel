#!/bin/bash
# Скрипт для деплоя Mini App на сервер

echo "🚀 Деплой Mini App на сервер..."

# 1. Остановить сервис
echo "⏸️  Останавливаем сервис..."
sudo systemctl stop miniapp

# 2. Обновить код (если используете git)
# git pull origin main

# 3. Установить зависимости
echo "📦 Устанавливаем зависимости..."
pip3 install -r requirements.txt

# 4. Проверить конфигурацию
echo "✅ Проверяем конфигурацию..."
python3 -c "from webapp.server import app; print('OK')"

# 5. Запустить сервис
echo "▶️  Запускаем сервис..."
sudo systemctl start miniapp
sudo systemctl status miniapp

# 6. Проверить логи
echo "📋 Последние логи:"
sudo journalctl -u miniapp -n 20 --no-pager

echo "✅ Деплой завершен!"

