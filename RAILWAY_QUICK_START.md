# 🚀 Быстрый старт на Railway

## ✅ Вы уже зарегистрировались через GitHub - отлично!

Теперь выполните следующие шаги:

## Шаг 1: Подготовьте проект для Git (если еще не сделали)

```powershell
cd "V:\Vladik Botik"

# Инициализируйте Git (если еще не сделали)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit - Telegram Mini App Bot"
```

## Шаг 2: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Название репозитория: `telegram-miniapp-bot` (или любое другое)
3. **НЕ** добавляйте README, .gitignore или лицензию
4. Нажмите **"Create repository"**

## Шаг 3: Подключите локальный проект к GitHub

```powershell
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/telegram-miniapp-bot.git
git branch -M main
git push -u origin main
```

## Шаг 4: Подключите репозиторий к Railway

1. Откройте https://railway.app
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий `telegram-miniapp-bot`
5. Railway автоматически начнет деплой

## Шаг 5: Настройте переменные окружения

1. В Railway нажмите на ваш сервис
2. Перейдите на вкладку **"Variables"**
3. Нажмите **"+ New Variable"** и добавьте:

```
BOT_TOKEN = ваш_токен_бота
ADMIN_ID = ваш_telegram_id
CHANNEL_ID = @ваш_канал
```

**⚠️ WEBAPP_URL пока не добавляйте** - добавим после получения домена

## Шаг 6: Получите домен

1. В Railway перейдите на **"Settings"**
2. Найдите раздел **"Domains"**
3. Нажмите **"Generate Domain"**
4. Railway создаст домен (например: `telegram-miniapp-bot-production.up.railway.app`)
5. **Скопируйте этот URL**

6. Вернитесь в **"Variables"** и добавьте:
```
WEBAPP_URL = https://ваш-домен.up.railway.app
```
(Замените на ваш реальный домен)

## Шаг 7: Проверьте деплой

1. Перейдите на вкладку **"Deployments"**
2. Дождитесь статуса **"Active"** (зеленый)
3. Если есть ошибки - проверьте логи

## Шаг 8: Проверьте работу

1. Откройте ваш домен в браузере:
   ```
   https://ваш-домен.up.railway.app
   ```
   Должна открыться страница Mini App

2. Обновите локальный `.env`:
   ```env
   WEBAPP_URL=https://ваш-домен.up.railway.app
   ```

3. Запустите бота локально:
   ```powershell
   py -3.11 main.py
   ```

4. Проверьте в Telegram:
   - Откройте бота
   - `/start`
   - Нажмите "📱 Открыть приложение"

## 🎉 Готово!

Теперь Mini App работает на Railway и автоматически обновляется при каждом push в GitHub!

## 📝 Полезные команды

**Обновить код:**
```powershell
git add .
git commit -m "Update"
git push
```
Railway автоматически задеплоит изменения!

**Посмотреть логи:**
- В Railway → ваш сервис → вкладка "Deployments" → выберите деплой → "View Logs"

**Перезапустить сервис:**
- В Railway → Settings → Restart

## ❓ Проблемы?

**Ошибка деплоя?**
- Проверьте логи в Railway
- Убедитесь, что все файлы закоммичены
- Проверьте, что `Procfile` существует

**Mini App не открывается?**
- Проверьте, что `WEBAPP_URL` правильный (с `https://`)
- Убедитесь, что деплой успешен
- Проверьте логи на ошибки

Подробная инструкция: см. `RAILWAY_DEPLOY.md`

