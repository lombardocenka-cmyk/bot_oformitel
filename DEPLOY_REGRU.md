# Размещение Mini App на поддомене reg.ru

## 📋 План действий

1. Создать поддомен (например: `miniapp.ваш-домен.ru`)
2. Настроить веб-сервер на поддомене
3. Запустить FastAPI приложение
4. Настроить HTTPS (обязательно для Telegram)
5. Обновить конфигурацию бота

## Шаг 1: Создание поддомена в reg.ru

### Через панель управления reg.ru:

1. Войдите в панель управления reg.ru
2. Перейдите в раздел **"Домены"** → **"DNS-зоны"**
3. Выберите ваш домен
4. Добавьте новую A-запись:
   - **Имя поддомена**: `miniapp` (или любое другое)
   - **Тип**: `A`
   - **Значение**: IP-адрес вашего сервера (тот же, что у основного сайта)
   - **TTL**: `3600` (или оставьте по умолчанию)

5. Сохраните изменения
6. Подождите 5-15 минут для распространения DNS

### Альтернативно через CNAME:

Если хотите использовать CNAME (указывает на основной домен):
- **Имя**: `miniapp`
- **Тип**: `CNAME`
- **Значение**: `ваш-домен.ru.` (с точкой в конце)

## Шаг 2: Подготовка сервера

### Вариант A: Если у вас есть доступ к серверу (VPS/выделенный сервер)

1. **Подключитесь к серверу по SSH**

2. **Создайте директорию для Mini App:**
```bash
mkdir -p /var/www/miniapp
cd /var/www/miniapp
```

3. **Загрузите файлы проекта:**
   - Загрузите папку `webapp` на сервер
   - Или используйте git для клонирования

4. **Установите Python и зависимости:**
```bash
# Установите Python 3.11 (если еще не установлен)
# Установите зависимости
pip3 install fastapi uvicorn python-multipart aiogram aiohttp aiosqlite beautifulsoup4 lxml python-dotenv
```

### Вариант B: Если используете виртуальный хостинг (shared hosting)

Для shared hosting обычно нужно:
1. Создать поддомен через панель управления
2. Загрузить файлы в папку поддомена
3. Настроить через `.htaccess` или панель управления

**⚠️ Внимание:** На shared hosting может не быть доступа к Python или возможности запускать долгоживущие процессы. В этом случае рассмотрите:
- Использование сервисов типа Railway, Render, Heroku
- Или аренду VPS

## Шаг 3: Настройка веб-сервера

### Вариант 1: Nginx + Gunicorn/Uvicorn (рекомендуется)

**Создайте конфигурацию Nginx:**

```nginx
server {
    listen 80;
    server_name miniapp.ваш-домен.ru;

    # Редирект на HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name miniapp.ваш-домен.ru;

    # SSL сертификаты (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/ваш-домен.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ваш-домен.ru/privkey.pem;

    # Статические файлы
    location /static/ {
        alias /var/www/miniapp/webapp/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Проксирование на FastAPI
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**Примените конфигурацию:**
```bash
sudo nginx -t  # Проверка конфигурации
sudo systemctl reload nginx
```

### Вариант 2: Apache (если используете Apache)

**Создайте виртуальный хост:**

```apache
<VirtualHost *:80>
    ServerName miniapp.ваш-домен.ru
    Redirect permanent / https://miniapp.ваш-домен.ru/
</VirtualHost>

<VirtualHost *:443>
    ServerName miniapp.ваш-домен.ru
    
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/ваш-домен.ru/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/ваш-домен.ru/privkey.pem

    # Статические файлы
    Alias /static /var/www/miniapp/webapp/static
    <Directory /var/www/miniapp/webapp/static>
        Require all granted
    </Directory>

    # Проксирование на FastAPI
    ProxyPreserveHost On
    ProxyPass /static !
    ProxyPass / http://127.0.0.1:8000/
    ProxyPassReverse / http://127.0.0.1:8000/
</VirtualHost>
```

## Шаг 4: Настройка SSL сертификата

**Установите Let's Encrypt (бесплатный SSL):**

```bash
# Установите certbot
sudo apt-get update
sudo apt-get install certbot python3-certbot-nginx  # для Nginx
# или
sudo apt-get install certbot python3-certbot-apache  # для Apache

# Получите сертификат для поддомена
sudo certbot --nginx -d miniapp.ваш-домен.ru
# или
sudo certbot --apache -d miniapp.ваш-домен.ru

# Автообновление (настроится автоматически)
```

## Шаг 5: Настройка systemd сервиса для FastAPI

**Создайте файл `/etc/systemd/system/miniapp.service`:**

```ini
[Unit]
Description=Mini App FastAPI Server
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/miniapp
Environment="PATH=/usr/bin:/usr/local/bin"
ExecStart=/usr/local/bin/uvicorn webapp.server:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Запустите сервис:**
```bash
sudo systemctl daemon-reload
sudo systemctl enable miniapp
sudo systemctl start miniapp
sudo systemctl status miniapp
```

## Шаг 6: Обновление конфигурации бота

**Обновите `.env` файл:**

```env
BOT_TOKEN=ваш_токен
ADMIN_ID=ваш_id
CHANNEL_ID=@ваш_канал
WEBAPP_URL=https://miniapp.ваш-домен.ru
```

## Шаг 7: Адаптация кода для продакшена

Создайте файл `webapp/server_prod.py` для продакшена:

```python
"""
Production конфигурация веб-сервера
"""
import os
from webapp.server import app

# Настройки для продакшена
if __name__ == "__main__":
    import uvicorn
    
    # Используйте переменные окружения для настройки
    host = os.getenv("HOST", "127.0.0.1")
    port = int(os.getenv("PORT", 8000))
    workers = int(os.getenv("WORKERS", 4))
    
    uvicorn.run(
        "webapp.server:app",
        host=host,
        port=port,
        workers=workers,
        log_level="info"
    )
```

## Шаг 8: Проверка работы

1. **Проверьте доступность поддомена:**
```bash
curl https://miniapp.ваш-домен.ru
```

2. **Проверьте статические файлы:**
```bash
curl https://miniapp.ваш-домен.ru/static/style.css
```

3. **Откройте в браузере:**
   - Перейдите на `https://miniapp.ваш-домен.ru`
   - Должна открыться страница Mini App

4. **Проверьте в Telegram:**
   - Откройте бота
   - Нажмите `/start`
   - Нажмите "📱 Открыть приложение"
   - Mini App должна открыться

## 🔧 Решение проблем

### Поддомен не открывается
- Проверьте DNS записи (может занять до 24 часов)
- Проверьте, что веб-сервер запущен
- Проверьте логи: `sudo journalctl -u miniapp -f`

### Ошибка SSL
- Убедитесь, что сертификат установлен: `sudo certbot certificates`
- Проверьте, что порт 443 открыт в firewall

### Mini App не работает
- Проверьте, что URL в `.env` правильный и начинается с `https://`
- Проверьте логи сервера
- Убедитесь, что все зависимости установлены

## 📝 Логи и мониторинг

**Просмотр логов:**
```bash
# Логи systemd сервиса
sudo journalctl -u miniapp -f

# Логи Nginx
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log
```

## 🔒 Безопасность

1. **Firewall:**
```bash
# Откройте только необходимые порты
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```

2. **Ограничьте доступ к API:**
   - Используйте rate limiting
   - Валидируйте все входящие данные
   - Используйте HTTPS только

3. **Обновляйте зависимости:**
```bash
pip3 list --outdated
pip3 install --upgrade package_name
```

## ✅ Готово!

После выполнения всех шагов ваш Mini App будет доступен по адресу:
`https://miniapp.ваш-домен.ru`

И будет работать изолированно от основного сайта!

