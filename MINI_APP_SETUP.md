# Настройка Telegram Mini App

## Что такое Mini App?

Mini App - это веб-приложение, которое открывается прямо в Telegram. Оно предоставляет более удобный интерфейс для создания постов с использованием HTML, CSS и JavaScript.

## Структура проекта

```
.
├── webapp/
│   ├── static/
│   │   ├── index.html    # Интерфейс приложения
│   │   ├── style.css     # Стили
│   │   └── app.js        # Логика приложения
│   ├── server.py         # FastAPI сервер
│   └── start_server.py   # Скрипт запуска
```

## Установка зависимостей

```powershell
py -3.11 -m pip install fastapi uvicorn python-multipart
```

## Настройка

### 1. Получите публичный URL для вашего веб-сервера

Telegram требует HTTPS для Mini Apps. Для разработки можно использовать:

**Вариант 1: ngrok (рекомендуется для разработки)**
```powershell
# Скачайте ngrok с https://ngrok.com/
# Запустите:
ngrok http 8000
# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
```

**Вариант 2: Деплой на сервер**
- Разместите приложение на сервере с HTTPS (например, Heroku, Railway, VPS)

### 2. Обновите .env файл

Добавьте в `.env`:
```
WEBAPP_URL=https://ваш-домен.com
```

Для локальной разработки с ngrok:
```
WEBAPP_URL=https://abc123.ngrok.io
```

### 3. Запуск

**Терминал 1: Запуск бота**
```powershell
py -3.11 main.py
```

**Терминал 2: Запуск веб-сервера**
```powershell
cd webapp
py -3.11 start_server.py
```

**Терминал 3: ngrok (если используете)**
```powershell
ngrok http 8000
```

## Использование

1. Откройте бота в Telegram
2. Отправьте команду `/start`
3. Нажмите кнопку "📱 Открыть приложение"
4. Mini App откроется прямо в Telegram
5. Следуйте инструкциям в приложении для создания поста

## Функционал Mini App

- ✅ Выбор категории товара
- ✅ Ввод названия товара
- ✅ Автоматический поиск характеристик
- ✅ Редактирование характеристик
- ✅ Загрузка фотографий (до 12 штук)
- ✅ Ввод ссылки на Авито
- ✅ Отправка поста на модерацию

## Безопасность

Mini App использует `initData` от Telegram для аутентификации пользователей. Данные валидируются на сервере с помощью `safe_parse_webapp_init_data`.

## Решение проблем

### Ошибка "Invalid init data signature"
- Проверьте, что `BOT_TOKEN` в `.env` правильный
- Убедитесь, что веб-сервер имеет доступ к `config.py`

### Mini App не открывается
- Проверьте, что URL в `.env` начинается с `https://`
- Убедитесь, что веб-сервер запущен и доступен
- Проверьте, что ngrok (если используется) работает

### Фотографии не загружаются
- В текущей версии фотографии сохраняются как base64
- Для продакшена рекомендуется загружать фото на сервер и сохранять file_id

## Дополнительные улучшения

1. **Загрузка фотографий на сервер**: Вместо base64 загружайте фото на сервер и получайте file_id
2. **Предпросмотр поста**: Показывайте предпросмотр перед отправкой
3. **История постов**: Добавьте просмотр ранее созданных постов
4. **Уведомления**: Push-уведомления о статусе модерации

