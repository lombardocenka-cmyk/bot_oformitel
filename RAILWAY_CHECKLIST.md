# ✅ Чеклист проверки Railway

## Проверьте следующее:

### 1. ✅ Переменные окружения установлены?
- `BOT_TOKEN` - ваш токен бота
- `ADMIN_ID` - ваш Telegram ID
- `CHANNEL_ID` - ваш канал
- `WEBAPP_URL` - домен от Railway (должен начинаться с `https://`)

### 2. ✅ Команда запуска настроена?
В Settings → Start Command должно быть:
```
cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT
```

### 3. ✅ Домен получен?
- В Settings → Domains должен быть домен типа `ваш-проект.up.railway.app`
- Этот домен добавлен в `WEBAPP_URL` в Variables

### 4. ✅ Деплой успешен?
- Перейдите на вкладку "Deployments"
- Последний деплой должен иметь статус "Active" (зеленый)
- Если статус "Failed" - нажмите на деплой и посмотрите логи

### 5. ✅ Mini App открывается?
- Откройте ваш домен в браузере: `https://ваш-домен.up.railway.app`
- Должна открыться страница Mini App

### 6. ✅ Бот настроен?
- Обновите локальный `.env` файл:
  ```
  WEBAPP_URL=https://ваш-домен.up.railway.app
  ```
- Запустите бота: `py -3.11 main.py`

### 7. ✅ Проверка в Telegram?
- Откройте бота
- Отправьте `/start`
- Должна появиться кнопка "📱 Открыть приложение"
- Нажмите на неё - Mini App должна открыться

## ❓ Если что-то не работает:

**Mini App не открывается в браузере?**
- Проверьте логи в Railway (Deployments → View Logs)
- Убедитесь, что деплой успешен

**Ошибка в логах?**
- Проверьте, что все переменные окружения установлены
- Убедитесь, что команда запуска правильная

**Кнопка не появляется в боте?**
- Проверьте, что `WEBAPP_URL` в локальном `.env` правильный
- Убедитесь, что бот запущен

