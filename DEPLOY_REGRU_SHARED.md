# Размещение Mini App на reg.ru Shared Hosting

Если у вас **shared hosting** (общий хостинг) без доступа к серверу, есть несколько вариантов:

## ⚠️ Ограничения Shared Hosting

На shared hosting обычно:
- ❌ Нет доступа к командной строке
- ❌ Нельзя запускать долгоживущие процессы (FastAPI/Uvicorn)
- ❌ Ограниченные возможности настройки

## ✅ Решения

### Вариант 1: Использовать внешний сервис (рекомендуется)

Разместите Mini App на бесплатном/платном сервисе:

**Бесплатные варианты:**
- **Railway** (railway.app) - бесплатный тариф
- **Render** (render.com) - бесплатный тариф
- **Fly.io** (fly.io) - бесплатный тариф
- **PythonAnywhere** (pythonanywhere.com) - бесплатный тариф

**Инструкция для Railway:**
1. Зарегистрируйтесь на railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Railway автоматически определит Python проект
5. Добавьте переменные окружения:
   - `BOT_TOKEN`
   - `ADMIN_ID`
   - `CHANNEL_ID`
6. Railway даст вам HTTPS URL автоматически
7. Добавьте этот URL в `.env` как `WEBAPP_URL`

### Вариант 2: Арендовать VPS

Если нужен полный контроль:
- **reg.ru VPS** - от 200₽/месяц
- **Timeweb VPS** - от 150₽/месяц
- **Selectel VPS** - от 200₽/месяц

Следуйте инструкции в `DEPLOY_REGRU.md`

### Вариант 3: Использовать статический хостинг + API

Если на shared hosting есть поддержка PHP/Python:

1. **Разместите статические файлы** (HTML/CSS/JS) на поддомене reg.ru
2. **Создайте API на внешнем сервисе** (Railway, Render)
3. **Настройте CORS** в API для работы с вашим доменом

**Пример структуры:**
```
miniapp.ваш-домен.ru/          → Статические файлы (на reg.ru)
api.ваш-домен.ru/              → FastAPI сервер (на Railway/Render)
```

## 📝 Быстрая инструкция для Railway

1. **Установите Railway CLI:**
```bash
# Windows
iwr https://railway.app/install.sh | iex

# Или скачайте с https://railway.app/cli
```

2. **Войдите:**
```bash
railway login
```

3. **Инициализируйте проект:**
```bash
cd "V:\Vladik Botik"
railway init
```

4. **Настройте переменные окружения:**
```bash
railway variables set BOT_TOKEN=ваш_токен
railway variables set ADMIN_ID=ваш_id
railway variables set CHANNEL_ID=@ваш_канал
```

5. **Деплой:**
```bash
railway up
```

6. **Получите URL:**
```bash
railway domain
# Скопируйте URL и добавьте в .env как WEBAPP_URL
```

## 🔄 Альтернатива: Использовать только бота

Если размещение Mini App сложно, можно использовать только бота:
- Бот работает без веб-сервера
- Все функции доступны через команды
- Не нужен поддомен или внешний сервис

Просто запустите:
```powershell
py -3.11 main.py
```

И используйте бота через команды `/start` и диалог.

## 💡 Рекомендация

Для начала используйте **Railway** или **Render** - это самый простой способ:
- ✅ Бесплатно
- ✅ Автоматический HTTPS
- ✅ Простой деплой
- ✅ Автоматические обновления

Потом, если нужно, можно перенести на свой сервер.

