# 🚀 Деплой на Render.com

## Шаг 1: Регистрация

1. Перейдите на https://render.com
2. Нажмите **"Get Started for Free"**
3. Войдите через **GitHub**

## Шаг 2: Создание Web Service

1. В Dashboard нажмите **"+ New"**
2. Выберите **"Web Service"**
3. Нажмите **"Connect account"** рядом с GitHub
4. Выберите репозиторий **`bot_oformitel`**
5. Нажмите **"Connect"**

## Шаг 3: Настройка сервиса

Заполните форму:

- **Name:** `telegram-miniapp` (или любое другое)
- **Region:** Выберите ближайший (например, Frankfurt)
- **Branch:** `main`
- **Root Directory:** (оставьте пустым)
- **Runtime:** `Python 3`
- **Build Command:** (оставьте пустым)
- **Start Command:** 
  ```
  cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT
  ```
- **Plan:** `Free` (бесплатный)

## Шаг 4: Переменные окружения

Прокрутите вниз до раздела **"Environment Variables"** и добавьте:

```
BOT_TOKEN = ваш_токен_бота
ADMIN_ID = ваш_telegram_id
CHANNEL_ID = @ваш_канал
```

**⚠️ WEBAPP_URL пока не добавляйте** - добавим после получения домена

## Шаг 5: Создание сервиса

1. Нажмите **"Create Web Service"**
2. Render начнет деплой автоматически
3. Дождитесь завершения (обычно 2-5 минут)

## Шаг 6: Получение домена

1. После успешного деплоя Render автоматически создаст домен
2. Домен будет в формате: `telegram-miniapp.onrender.com`
3. **Скопируйте этот URL**

4. Вернитесь в **Environment Variables**
5. Добавьте:
   ```
   WEBAPP_URL = https://telegram-miniapp.onrender.com
   ```
   (Замените на ваш реальный домен)

6. Render автоматически перезапустит сервис

## Шаг 7: Проверка работы

1. **Откройте домен в браузере:**
   ```
   https://telegram-miniapp.onrender.com
   ```
   Должна открыться страница Mini App

2. **Обновите локальный .env:**
   ```env
   WEBAPP_URL=https://telegram-miniapp.onrender.com
   ```

3. **Запустите бота:**
   ```powershell
   py -3.11 main.py
   ```

4. **Проверьте в Telegram:**
   - Откройте бота
   - `/start`
   - Нажмите "📱 Открыть приложение"

## ⚠️ Важно про Render Free Plan

- **Спиндаун:** Бесплатные сервисы "засыпают" после 15 минут неактивности
- **Первый запрос:** Может занять 30-60 секунд для "пробуждения"
- **Решение:** Используйте платный план ($7/месяц) или настройте cron для поддержания активности

## 🔧 Настройка для предотвращения спиндауна

Создайте файл `render_keepalive.py`:

```python
import requests
import time
import os

WEBAPP_URL = os.getenv("WEBAPP_URL", "")

if __name__ == "__main__":
    while True:
        try:
            requests.get(WEBAPP_URL, timeout=10)
            print("Keepalive ping sent")
        except:
            pass
        time.sleep(600)  # Каждые 10 минут
```

Или используйте внешний сервис типа UptimeRobot для пинга каждые 5 минут.

## ✅ Готово!

Теперь Mini App работает на Render!

## 📝 Обновление кода

При каждом push в GitHub Render автоматически задеплоит изменения:
```powershell
git add .
git commit -m "Update"
git push
```

## ❓ Проблемы?

**Ошибка деплоя?**
- Проверьте логи в Render Dashboard
- Убедитесь, что Start Command правильный
- Проверьте, что все зависимости в `requirements.txt`

**Mini App не открывается?**
- Проверьте, что деплой успешен (зеленый статус)
- Убедитесь, что `WEBAPP_URL` правильный
- Подождите 30-60 секунд (если сервис "спал")

