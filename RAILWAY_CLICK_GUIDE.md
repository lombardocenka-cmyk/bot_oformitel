# 🖱️ Куда нажимать в Railway

## Сейчас на экране:

Вы видите проект "thorough-simplicity" с сервисом "web", который еще не задеплоен.

## Что делать:

### Вариант 1: Настройте существующий сервис "web"

1. **Нажмите на карточку "web"** в центре экрана
2. Откроется настройка сервиса
3. Перейдите на вкладку **"Settings"** или **"Variables"**
4. Добавьте переменные окружения (см. ниже)

### Вариант 2: Создайте новый сервис

1. **Нажмите кнопку "+ Create"** справа вверху (рядом с "Activity")
2. Выберите **"GitHub Repo"** или **"Empty Service"**
3. Если выбираете GitHub Repo:
   - Найдите репозиторий `bot_oformitel`
   - Railway автоматически начнет деплой

## Настройка переменных окружения:

После открытия сервиса:

1. Перейдите на вкладку **"Variables"** (слева в меню)
2. Нажмите **"+ New Variable"**
3. Добавьте:
   ```
   BOT_TOKEN = ваш_токен_бота
   ADMIN_ID = ваш_telegram_id
   CHANNEL_ID = @ваш_канал
   ```

## Настройка команды запуска:

1. Перейдите на вкладку **"Settings"**
2. Найдите раздел **"Start Command"**
3. Установите:
   ```
   cd webapp && python -m uvicorn server:app --host 0.0.0.0 --port $PORT
   ```

## Получение домена:

1. В настройках сервиса перейдите на вкладку **"Settings"**
2. Найдите раздел **"Domains"**
3. Нажмите **"Generate Domain"**
4. Скопируйте URL
5. Добавьте в Variables: `WEBAPP_URL = https://ваш-домен.up.railway.app`

## Проверка деплоя:

1. Перейдите на вкладку **"Deployments"**
2. Дождитесь статуса **"Active"** (зеленый)
3. Если есть ошибки - нажмите на деплой и посмотрите логи

## 🎯 Рекомендация:

**Начните с нажатия на карточку "web"** - это откроет настройки сервиса, где вы сможете:
- Добавить переменные окружения
- Настроить команду запуска
- Получить домен

